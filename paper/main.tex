% Paper A -- RoboPAD short track (4 pp) / VLM4RWD (8 pp)
%
% Build:  pdflatex main && bibtex main && pdflatex main && pdflatex main
%
% REQUIRES neurips_2026.sty in this directory (download from neurips.cc; it is not
% redistributable here). Without it LaTeX will fail on the \usepackage line below.
%
% Track option: dblblindworkshop = workshop with double-blind review, which is what
% RoboPAD, VLM4RWD and Interpretability-for-Discovery all use. Switch to
% [dblblindworkshop, final] only after acceptance.
% ---------------------------------------------------------------------------
% ONE SOURCE, TWO PAPERS.
%
% RoboPAD wants 4 pages, VLM4RWD wants 8. Maintaining two .tex files guarantees
% they drift -- a number fixed in one and missed in the other is exactly the
% failure this project has spent a fortnight learning to avoid. So the long-only
% material lives inside \iflong ... \fi and both versions build from this file.
%
%   4-page  (RoboPAD):  \longfalse   <- default below
%   8-page  (VLM4RWD):  \longtrue
% ---------------------------------------------------------------------------
\newif\iflong
\longfalse          % <-- flip to \longtrue for the 8-page VLM4RWD version

\documentclass{article}
\usepackage[dblblindworkshop]{neurips_2026}

% The workshop template requires BOTH \title and \workshoptitle. The venue differs
% between the two builds, so it follows the same switch.
\iflong
  \workshoptitle{Vision-Language Models for Real-World Deployment (VLM4RWD)}
\else
  \workshoptitle{Robot Foundation Models: Post-Pretraining Adaptation, Reasoning and Evaluation (RoboPAD)}
\fi

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{amsfonts}
\usepackage{nicefrac}
\usepackage{microtype}
\usepackage{xcolor}
\usepackage{graphicx}

\title{What Compositional Grounding Failure in VLAs Is \emph{Not}:\\
Two Nulls, a Substitution Signature, and the Controls Between Them}

\author{%
  Anonymous \\
  % Filled in for the camera-ready only; submission is double-blind.
}

\begin{document}
\maketitle

\begin{abstract}
That vision-language-action models fail on undemonstrated object--destination pairings is
already established~\citep{brittle,scaleup}. We ask what that failure is \emph{not}, and what it
costs to measure it correctly. Using minimal contrastive pairs on LIBERO-Goal---identical
observations, instructions differing in exactly one referent---we find the failure is narrower
than a vision-dominance account predicts. When the arm already carries an object toward one goal
and is commanded to another, so the visual context actively opposes the instruction, a
LIBERO-finetuned SmolVLA redirects on $0.946$ $[0.917, 0.971]$ of trials; on neutral states it
follows at $0.745$ $[0.703, 0.788]$. Language wins in precisely the regime where it is reported
to lose~\citep{notallfeatures}. Failure appears only under composition, and it is structured:
$93$--$96\%$ of errors send the arm to a destination that object \emph{was} trained with---a
substitution, not a lapse of attention. We then report two negative results together with the
controls that establish them. A 130-site causal patching sweep appears to localize the failure
until an identical sweep on a contrast with \emph{no} failure to recover reproduces the same
profile; $0$ of $130$ sites survive correction. A linear probe finds the instruction encoded in
the backbone but gives expert-level results that invert between checkpoints. We release the
stimulus generator, a checkpoint competence gate---only $2$ of $8$ public checkpoints are
usable---and five measurement artifacts that produced clean, significant, wrong numbers in our
own pipeline.
\end{abstract}

\section{Introduction}
\label{sec:intro}

VLAs map a camera image and a language instruction to robot actions. A growing behavioural
literature reports that they under-use the language and default to visual
priors~\citep{better,icbench,liberocf,robosem}. This matters because the instruction is the
entire control interface: a policy that ignores the words is not steerable.

\citet{brittle} establish the compositional case directly on SmolVLA: held-out
object--location pairings fail because success rests on correlations that do not transfer.
\citet{scaleup} report the same across six models. We take that finding as given. Our
question is what it is \emph{not}---which competing accounts of the failure survive contact
with a control---and what it costs to measure such things without fooling yourself.

That second question is not rhetorical. Every intuitive measurement we built here returned a
clean, significant, plausible number that later proved to be an artifact, and each was caught
only by a check capable of contradicting it.

We probe with minimal contrastive pairs: identical observations, instructions differing in
exactly one referent, so any change in the predicted action is attributable to the swapped
word. Three regimes of increasing linguistic demand separate the accounts. A vision-dominance
account predicts failure when the visual context opposes the instruction; we find the opposite,
with the policy redirecting mid-trajectory on $94\%$ of trials. An attention-lapse account
predicts unstructured error; we find $93$--$96\%$ of errors landing on a destination that object
\emph{was} trained with. What remains is substitution of a memorised pairing---an
object$\leftrightarrow$destination \emph{binding} failure.

\paragraph{Contributions.}
\begin{enumerate}\itemsep0pt
\item Evidence that the failure is \emph{not} vision-dominance: under direct visual--motor
  conflict the policy follows language at $0.946$, in the regime where it is reported to be
  ignored~\citep{notallfeatures}.
\item A substitution signature ($93$--$96\%$ of errors on a memorised destination for that
  object), characterising the structure of a failure previously reported only as a rate.
\item A checkpoint competence gate: only 2 of 8 public LIBERO SmolVLA checkpoints are usable,
  and four score worse than predicting an unrelated trajectory.
\item Two negative results shipped \emph{with} the controls that establish them, and five
  measurement artifacts that produced clean, significant, wrong numbers in our own pipeline.
\item A released, gate-validated contrastive stimulus generator, plus a reference-free
  directional readout that makes undemonstrated commands scoreable.
\end{enumerate}

\section{Related work}
\label{sec:related}

\paragraph{The compositional failure is established; we build on it.} \citet{brittle} show
held-out object--location pairings fail on SmolVLA and $\pi_{0.5}$, and \citet{scaleup} find
memorised factor associations across six models. Both are behavioural and report the failure as
a rate. We add what a rate cannot distinguish: which mechanism produces it, and which competing
accounts a control rules out. \citet{better,icbench,liberocf,robosem} report instruction
under-use more broadly.

\paragraph{Interpretability of VLA grounding.} \citet{notallfeatures} run activation injection
on six models including SmolVLA and report that language sensitivity depends on task structure,
with a probe decoding a prompt at $99.3\%$ while behaviour ignores it. That probe result is
$\pi_{0.5}$-specific and correlational; our attempt to reproduce the analogous result on SmolVLA
inverted between checkpoints, which we report rather than resolve. Their layer interventions act
on visual and full-pathway activations, so where \emph{language} influence is lost remains open;
they name compositional instructions as future work.

\paragraph{Compositional generalisation.} Studied extensively in language-only and navigation
settings~\citep{scan,gscan}, and much less in continuous-control policies whose output is a
trajectory rather than a token sequence.

\paragraph{Binding.} Work on binding identifiers~\citep{bindingids,vlmbinding} predicts exactly
the object$\leftrightarrow$attribute failure we observe, but characterises it mechanistically on
the language-model side; the behavioural signature in an action policy has not been described.

\paragraph{VLA interpretability.} \citet{notallfeatures,vlatrace} localize features in VLAs.
Such methods need a condition that reliably elicits the failure, which is what we supply. Our
negative sweep also speaks to how readily these methods produce artifacts without matched
controls.

\section{Method}
\label{sec:method}

\paragraph{Minimal contrastive pairs.} One observation, two instructions differing in exactly one
referent. A pair ships only if it passes four gates, each with a regression test: a single-edit
word-level diff (one edit block, merged across filler words, preposition-free span, verb
unchanged); shared scene; both instructions achievable from the state; and counterbalanced source
tasks. Two of these gates exist because naive implementations silently admitted invalid pairs.

\paragraph{Three regimes.} \emph{Neutral}: pre-grasp states, where both instructions are
achievable and no visual prior favours either. \emph{Conflict}: post-grasp states, where the arm
already carries the object toward the demonstrated goal so the observation opposes the
alternative command. \emph{Compositional}: trained versus never-demonstrated pairings. All ten
LIBERO-Goal scenes contain the same seven objects, so novel pairings are physically executable.

\paragraph{Readouts.} For the first two regimes, \emph{directional IFR}: does commanding the
non-demonstrated instruction push the prediction away from the demonstrated trajectory (chance
$0.5$)? For the compositional regime, novel commands have no demonstration, so we score which
\emph{destination anchor} the predicted net displacement points toward. Anchors are the mean
final end-effector position of the demonstrations ending at each destination (sd
$0.03$--$0.05$\,m, pairwise separation $0.19$--$0.57$\,m; the cabinet anchor agrees within
$0.07$\,m across two different objects, so it tracks the destination rather than the task).

\paragraph{Controls.} A competence gate (Sec.~\ref{sec:exp}); pinned flow-matching noise, so
identical inputs give bit-identical actions (control passes at exactly $0.0$); a trained-pairing
control for the compositional readout; and a fixed-state control holding the observation
byte-identical while varying only the object word.

\section{Experiments}
\label{sec:exp}

\paragraph{Setup.} SmolVLA-450M~\citep{smolvla} on LIBERO-Goal~\citep{libero}, evaluated
offline on stored demonstration states. Paired bootstrap, 10k resamples. We use SmolVLA
because its small size makes exhaustive patching tractable on a single consumer GPU; the
discrete action vocabulary of larger autoregressive VLAs such as
OpenVLA~\citep{openvla} would permit a direct action lens, which we discuss as future work.

\paragraph{Competence gate.} Only 2 of 8 public LIBERO SmolVLA checkpoints track the
demonstrations better than an unrelated trajectory does; four score \emph{worse} than that
baseline at every image orientation. All results below use the two that pass. We report this as
a result rather than housekeeping: it bears directly on the reproducibility of this literature.

\begin{table}[t]
\caption{Grounding holds under neutral and conflicting conditions and breaks under composition.
Compositional figures are the paired fixed-state control, which holds the observation
byte-identical and varies only the object word.}
\label{tab:regimes}
\centering
\begin{tabular}{llll}
\toprule
Regime & Readout & Checkpoint A & Checkpoint B \\
\midrule
Neutral ($n{=}400$)        & directional IFR & $0.745$ $[0.703, 0.788]$ & --- \\
Conflict ($n{=}240$)       & directional IFR & $0.946$ $[0.917, 0.971]$ & $0.929$ $[0.896, 0.958]$ \\
Compositional ($n{=}800$)  & destination gap & $+0.250$ $[0.197, 0.300]$ & $+0.193$ $[0.143, 0.243]$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{The failure is compositional.} Table~\ref{tab:regimes}. Under direct visual--motor
conflict the policy redirects on command. Under an undemonstrated pairing of familiar words it
does not, and the gap is significant in all six destination\,$\times$\,checkpoint cells.
Crucially, $93$--$96\%$ of novel-command errors send the arm to a destination that object
\emph{was} trained with: the policy confidently executes a memorised pairing rather than
wandering.

\paragraph{Causal patching does not localize it.} Patching all 130 sites
($\{\textrm{vlm},\textrm{expert}\}\times L_{0..15}\times\{\textrm{resid\_pre},
\textrm{attn\_out},\textrm{mlp\_out},\textrm{resid\_post}\}$) from a working run into a failing
one yields an apparently clean picture: VLM influence decaying with depth, expert influence
rising toward the output. It is an artifact of causal proximity. Running the identical sweep on a
\emph{control} contrast---both instructions trained, no failure to recover---reproduces the same
profile (Fig.~\ref{fig:sweep}). Novel minus control has mean $-0.056$, and \textbf{0 of 130 sites
survive Benjamini--Hochberg correction} in either run.

\paragraph{Probing: encoded, but no replicable mechanism.} A linear probe trained on trained
pairings and tested on novel ones, with a label-shuffled control at every site and a
state-grouped split, decodes the named destination in the VLM at $1.00$ on both checkpoints
(controls at chance). The instruction is encoded and survives the backbone---though this is
close to definitional, since the destination word is present in the input tokens.

Inside the \emph{action expert} the picture does not replicate (Fig.~\ref{fig:probe}). One
checkpoint has 37 expert sites decoding novel pairings above $0.5$, peaking at $0.985$; the
other has 2, with 29 sites \emph{below} $0.10$ against a chance level of $0.25$---reliably
wrong rather than uninformative. \textbf{We therefore assert no readout-versus-encoding
verdict.} An earlier draft did, on one checkpoint alone.

Systematic sub-chance decoding is a lead rather than noise: with four balanced classes it
requires a consistent mapping to a wrong class. If the expert represents the destination the
policy will \emph{actually} move toward---the memorised substitute---rather than the one named,
a probe trained where the two coincide would predict the substitute and be reliably wrong
exactly where we observe it, matching the behavioural substitution result. Testing this means
decoding the executed destination rather than the named one, which we leave to future work.

\begin{figure}[t]
\centering
\includegraphics[width=0.48\linewidth]{figures/fig2_sweep.pdf}
\hfill
\includegraphics[width=0.48\linewidth]{figures/fig3_probe.pdf}
\caption{\textbf{Left:} the patching sweep's apparent structure is reproduced by a control with
no failure to recover; their difference is flat. \textbf{Right:} the named destination is
decodable inside the action expert on one checkpoint but not the other; the second shows
systematic \emph{sub-chance} decoding, so we assert no mechanism.}
\label{fig:sweep}
\label{fig:probe}
\end{figure}

\section{Conclusion}
\label{sec:conclusion}

Grounding in this VLA does not fail globally---it fails at composition. Under direct
visual--motor conflict the policy still redirects on command; under an undemonstrated pairing of
familiar words it substitutes a memorised destination. Causal patching did not localize this, and
a matched control shows the sweep's apparent structure is proximity rather than mechanism.
Probing instead finds the destination present in the action expert and unused.

\paragraph{Limitations.} Effect sizes are moderate and halved under the strictest control. Both
competent checkpoints come from a single uploader, so replication is weak. LIBERO-Goal supports
one object contrast, so results generalise over states rather than referents. Evaluation is
offline, so ``correct'' means heading toward the named destination rather than task success.
Finally, the probe decodes the named \emph{destination}, which is present in the input tokens; we
have not shown that the object$\leftrightarrow$destination \emph{binding} itself is represented.

\iflong
\section{Five measurement artifacts}
\label{sec:artifacts}

Every result in this paper had a predecessor that was wrong. We report them because four of the
five produced clean, statistically significant, entirely plausible numbers, and because the
property that caught them generalises: each was exposed by a check \emph{capable of
contradicting the headline}, never by re-reading code.

\paragraph{1. One-sided stimulus set.} The generator consumed task A's states before reaching
task B, and callers never took enough pairs to get there, so 100\% of states came from the
A-side task. The directional readout then only ever asked its question in one direction, and any
asymmetry between the two instructions read as a grounding effect. It produced a striking
destination-versus-object dissociation. \emph{Caught by} a second checkpoint returning the
opposite, equally significant result on identical stimuli.

\paragraph{2. Inverted images.} LIBERO stores frames under MuJoCo's OpenGL convention. The
policy saw upside-down scenes and scored no better than predicting an unrelated trajectory,
while still yielding confident instruction-following numbers. \emph{Caught by} a competence
gate---the ratio of prediction-to-demonstration distance against the distance between two
unrelated demonstrations---which we now run before any experiment.

\paragraph{3. A reference that rewarded stalling.} The directional readout compared a 50-step
predicted chunk against a \emph{single} demonstration action broadcast across all 50 steps,
scoring a policy that stops as highly as one that follows the trajectory through. \emph{Caught
by} asking what the metric would return for a degenerate policy.

\paragraph{4. A leaking probe split.} Each observation contributes eight examples (two objects
$\times$ four destinations); splitting by example put near-identical activations in both train
and test. Held-out accuracy read $1.000$ at \emph{every} site. \emph{Caught by} a site we could
verify carries no signal: \texttt{expert.L0.resid\_pre} is byte-identical across instructions,
so perfect decoding there is impossible. Grouping by observation dropped it to $0.400$, equal to
its own shuffled control.

\paragraph{5. A transplant that moved the wrong thing.} The injected delta was taken between
instructions differing in the \emph{object} word, so it encoded object identity alongside any
binding; injecting it degraded behaviour monotonically with dose while a norm-matched random
direction stayed near zero. Restricting to language-token positions localises this rather than
removing it, since the object token sits inside the language block. No dose or position slice
fixes it. \emph{Not yet fixed}: the design that would separate the two holds the words fixed and
contrasts trials where the same novel instruction succeeds against trials where it fails.

\paragraph{Determinism, and which backend to distrust.} Because flow-matching noise is pinned
and the instrumented forward is exercised against the stock implementation, the pipeline is
exactly reproducible: the full 130-site sweep run on CPU and on an A100 agrees to
\textbf{$0.0000$ maximum absolute difference across all 130 sites}. That matters for reading
the one backend that does \emph{not} agree---on Apple MPS we measured attention and MLP sites
diverging from CPU by up to $0.21$, while residual-stream sites agreed to $0.014$. Exact
CPU/CUDA agreement localises that to the MPS kernels rather than to the method, which is the
difference between a caveat that bounds a result and one that merely worries about it. Anyone
running interpretability sweeps on Apple silicon should check this before trusting per-site
numbers.

\paragraph{What generalises.} A layer $\times$ component heatmap offers fewer opportunities to
visibly contradict itself than a scalar does. We would now require, before interpreting any such
map: a competence gate on the checkpoint, a matched control condition in which the effect being
localized does not exist, a replication checkpoint, and---for any direction-based
readout---conditioning on whether the instruction could move the action at all. That last one
matters because a site where language \emph{cannot} matter looks identical to a site where
language \emph{is ignored}.

\section{Extended results}
\label{sec:extended}

\paragraph{Per-destination compositional gaps.} The fixed-state control, which holds the
observation byte-identical and varies only the object word, gives positive gaps in all six
destination $\times$ checkpoint cells: the plate $+0.100 / +0.180$, the stove $+0.330 / +0.140$,
the rack $+0.320 / +0.260$. Pooled, $+0.250$ $[0.197, 0.300]$ and $+0.193$ $[0.143, 0.243]$.

\paragraph{Conflict strength.} Binning the conflict regime by how far the demonstration has
committed shows an apparent decline in the most-committed bin ($0.950 \to 0.900$ and
$0.975 \to 0.783$). This is a metric artifact rather than an effect: instruction sensitivity
collapses in the same bin, and where the instruction barely moves the action its \emph{direction}
is estimated from almost no signal. Splitting that bin by sensitivity separates them---at high
sensitivity, accuracy is $1.000$ and $0.949$.

\paragraph{Checkpoint screen.} Of eight public LIBERO SmolVLA checkpoints with a compatible
schema, two are competent (ratios $0.632$, $0.654$), one is marginal ($0.808$), four score worse
than an unrelated trajectory ($1.673$--$1.868$), and one fails to load. The four failures are not
a preprocessing artifact on our side: re-running two of them across all four image orientations
leaves them near $1.8$ regardless, and one is flat to three decimals across orientations, meaning
the image barely influences its output.
\fi

\bibliographystyle{plainnat}
\bibliography{refs}

\end{document}
